/* import { getParksDetails } from "../TransientState.js";

export const displayParkDetails = async () => {
  const parksDropdown = document.querySelector("#parks");
  const selectedParkId = parksDropdown.value;
  
  if (selectedParkId !== "0") {
    const parkDetails = await getParksDetails(selectedParkId);
    
    // Display the park details on the page
    // You can use DOM manipulation methods to show the details in a specific section or container.
  }
};

 */